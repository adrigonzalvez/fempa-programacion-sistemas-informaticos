package fempa.bingoserver;

import java.net.URI;
import java.net.http.WebSocket;
import java.net.http.HttpClient;
import java.util.concurrent.ExecutionException;

import com.sun.net.httpserver.HttpServer;

import java.util.concurrent.CompletableFuture;

public class BingoServer {

	public static void main(String[] args) {
		String ENDPOINT = "ws://localhost:8080";
		HttpClient client = HttpClient.newHttpClient();
		CompletableFuture<WebSocket> ws = client.newWebSocketBuilder().buildAsync(URI.create(ENDPOINT), new Listener() {
			
		});
		WebSocket server = ws.join();
		
		
		try {
			//server.sendText(ENDPOINT, false);
			
		} catch (Exception ex) {
			
		}

		
		
		

	}
	
	

}
